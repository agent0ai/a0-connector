name: a0_connector
title: A0 Connector
description: Current Agent Zero connector plugin for HTTP plus /ws integration, using API-key auth via mcp_server_token and handler activation through auth.handlers.
version: 0.1.0
settings_sections:
  - external
  - developer
per_project_config: false
per_agent_config: false
